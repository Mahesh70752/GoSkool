@model GoSkool.Views.Exam.ExamModel;

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>ExamEntity</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="@Model.Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="@Model.ExamDate" class="control-label"></label>
                <input asp-for="ExamDate" class="form-control" />
                <span asp-validation-for="ExamDate" class="text-danger"></span>
            </div>
            <input asp-for="@Model.subjectId" type="number" class="form-control" placeholder="@Model.subjectId" />
            <div class="form-group">
                <select asp-for="@Model.ClassId" asp-items="@Model.classList" class="form-select">
                    <options><option selected disabled>---Select Class---</option></options>
                </select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
